/**
 * Repository Index - Export all repository classes
 */

const ProxyRepository = require('./proxy_repository');
const ProfileRepository = require('./profile_repository');

module.exports = {
    ProxyRepository,
    ProfileRepository
};